/**
 * Setup global de Vitest.
 * Importa los matchers de jest-dom para aserciones de DOM más expresivas
 * (ej: expect(el).toBeInTheDocument(), toHaveTextContent(), etc.)
 */
import '@testing-library/jest-dom';
