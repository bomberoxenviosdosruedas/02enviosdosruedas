Una línea: el input de newsletter en píldora, con el botón amarillo adentro del mismo marco.

```jsx
<SubscribeForm onSubmit={email=>console.log(email)} />
```

Todo el contenedor es la píldora: el input no tiene borde propio.
