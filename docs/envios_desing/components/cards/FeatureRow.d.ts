import * as React from 'react';
/** Fila de beneficio sobre fondo blanco — el borde azul sólo aparece al hover. */
export interface FeatureRowProps { icon: string; title: string; children?: React.ReactNode; style?: React.CSSProperties; }
export declare function FeatureRow(props: FeatureRowProps): JSX.Element;
