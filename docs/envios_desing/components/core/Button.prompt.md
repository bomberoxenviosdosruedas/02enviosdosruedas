Una línea: el botón sólido del sistema — amarillo sobre azul, Bebas Neue en mayúscula.

```jsx
<Button variant="default" icon="Zap">Cotizar ahora</Button>
<Button variant="outline">Ver servicios</Button>
```

Para el CTA principal del hero usá `CtaPill` (píldora con ícono anidado), no este botón. `outlineBlue` es la única variante pensada para fondo blanco.
