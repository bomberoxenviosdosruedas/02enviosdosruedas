Una línea: cada sección arranca con esto — píldora, titular Anton y segunda línea amarilla.

```jsx
<SectionHeading onDark badge="Nuestros Servicios" title="Soluciones logísticas" highlight="A tu medida" underline />
```

Sobre blanco usá `onDark={false}` (el título sale `--brand-ink`). El titular nunca lleva punto final.
