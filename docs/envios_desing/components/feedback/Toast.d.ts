import * as React from 'react';
/** Confirmación en píldora, abajo a la derecha, con entrada de resorte. */
export interface ToastProps { message?: string; show?: boolean; icon?: string; }
export declare function Toast(props: ToastProps): JSX.Element;
