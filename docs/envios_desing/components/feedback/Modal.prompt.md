Una línea: el diálogo blanco que abre el detalle de cada servicio.

```jsx
<Modal open={!!sel} title="Envíos Express" badge="Urgente" onClose={close}
  footer={<Button block icon="ArrowRight">Cotizá tu Express</Button>}>Servicio de mensajería urbana inmediata…</Modal>
```

Panel blanco con borde azul de 2px y sombra dura — no uses una tarjeta oscura acá.
